Program id_errors(input, output);
Const
  b = 1;

Var
  // 重复定义
  a, x, x, y: integer;
  // 重复定义
  a: integer;
  // 类型错误
  z: int;
  // 变量定义语句错误
  a = 10;
  b = a;
Begin
  // 运算对象缺失
  x := 3 +;
  // 未定义标识符
  read(r, y)
End.
